import math
def find_next_square(sq):
    # Return the next square if sq is a square, -1 otherwise
    if sq<0:
        return None
    root = int(math.sqrt(sq))
    if root*root==sq:
        return (root+1)**2
    else:
        return -1
