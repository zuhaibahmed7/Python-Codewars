def duplicate_encode(word):
    #your code here
    word = word.lower()
    newStr = ""
    for chr in word:
        if word.count(chr)>1:
            newStr+=")"
        elif word.count(chr)==1:
             newStr+="("
    
    return newStr 
            
        
